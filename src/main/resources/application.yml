server:
  port: 8000
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/TP06
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true